[Unit]
Description=LunchDrop App
After=docker.service
Requires=docker.service

[Service]
WorkingDirectory=/home/kmit/KinLaiD
ExecStart=/usr/bin/docker compose up
ExecStop=/usr/bin/docker compose down
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
